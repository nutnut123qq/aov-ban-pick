"use client"

import { Tab, Tabs, TabsProps } from "@heroui/react"
import React from "react"

export const TedoTabs = (props: TabsProps) => {
    return <Tabs {...props} />
}
export const TedoTab = Tab
