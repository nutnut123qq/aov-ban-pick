"use client"

import { Select, SelectItem, SelectProps } from "@heroui/react"
import React from "react"

export const AovSelect = (props: SelectProps) => {
    return <Select {...props} />
}
export const AovSelectItem = SelectItem
