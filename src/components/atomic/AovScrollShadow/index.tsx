"use client"

import { ScrollShadow, ScrollShadowProps } from "@heroui/react"
import React from "react"

export const AovScrollShadow = (props: ScrollShadowProps) => {
    return <ScrollShadow {...props} />
}
