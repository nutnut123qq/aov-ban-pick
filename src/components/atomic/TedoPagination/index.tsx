"use client"

import { Pagination, PaginationProps } from "@heroui/react"
import React from "react"

export const TedoPagination = (props: PaginationProps) => {
    return <Pagination {...props} />
}
