"use client"

import { Tab, Tabs, TabsProps } from "@heroui/react"
import React from "react"

export const AovTabs = (props: TabsProps) => {
    return <Tabs {...props} />
}
export const AovTab = Tab
