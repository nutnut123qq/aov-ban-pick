"use client"

import { Select, SelectItem, SelectProps } from "@heroui/react"
import React from "react"

export const TedoSelect = (props: SelectProps) => {
    return <Select {...props} />
}
export const TedoSelectItem = SelectItem
